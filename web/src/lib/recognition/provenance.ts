/** Bump when RecognitionEvidence field meaning or normalization changes. */
export const EVIDENCE_SCHEMA_VERSION = 1;

/** Bump when rule/verifier decision semantics change incompatibly. */
export const RECOGNITION_VERSION = 'rules-v1';
