# Was `Disallow: /` (blocking the whole site from every crawler) — fine while there was nothing
# public to find, but marketing pages are a discoverable surface. The app itself (everything else
# under /) stays disallowed: it's an interactive product behind a camera prompt, not content to
# rank — see docs on this decision in CLAUDE.md-adjacent SEO notes. Add each new static marketing
# page here explicitly as it ships (matches the pattern already used for CSP headers in
# vercel.json — both files need the same new-page entry).
User-agent: *
Allow: /landing.html
Allow: /asl-alphabet.html
Allow: /$
Disallow: /

Sitemap: https://aslgame.vercel.app/sitemap.xml
