# ML pipeline deps (Phase C). The extractor (C.1) only needs the root requirements
# (mediapipe + opencv + numpy); these are for the dataset/training side.
#
# IMPORTANT: use a Python 3.11/3.12 venv — TensorFlow has no wheels for 3.14 (the
# MediaPipe venv here is 3.14, hence the separate .venv-ml).
#
# train.py sets TF_USE_LEGACY_KERAS=1 so the saved model is Keras 2 and converts to TF.js.
#
# On Kaggle/Colab (Linux) the TF.js export is clean: `pip install tensorflowjs`.
# On Windows tensorflowjs's deps (uvloop/tensorflow_decision_forests) don't build; install
# the converter without them and let train.py stub the unused TFDF import:
#   pip install --no-deps tensorflowjs tensorflow-hub
#   pip install tf_keras jax jaxlib
numpy>=1.26
matplotlib>=3.8
tensorflow>=2.15
tf_keras>=2.15        # Keras 2 API the tensorflowjs converter needs
tensorflowjs>=4.17    # browser export for the disambiguation layer (C.5)

# Dataset extraction tools (tools/wlasl_extract.py, tools/msasl_extract.py,
# tools/hmdb51_extract.py) — video/metadata fetching, not needed for training itself.
requests>=2.31
yt-dlp>=2024.1
huggingface_hub>=0.20  # HMDB51 mirror fetch — see ml/README.md's HMDB51 section
